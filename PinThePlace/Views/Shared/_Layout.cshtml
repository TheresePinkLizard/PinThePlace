<!--Purpose: _Layout.cshtml is a special Razor file that defines a common layout
(template) for your web pages. It typically includes the HTML structure, such as the
<head>, <header>, <footer>, and any common scripts or styles.

Usage: By using a layout, you ensure that all your pages share the same structure and
design. Individual views will inject their content into the layout’s RenderBody()
placeholder. -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Pin The Place</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="~/js/bootstrap.js"></script>
    <script src="~/js/jquery.min.js"></script>
    <link href="~/mystyle.css" rel="stylesheet" />
</head>
<body>
    <header class="background">
       <nav class="navbar navbar-expand-lg navbar-light bg-light background">
           <div class="container d-flex justify-content-between background">
               <ul class="navbar-nav">
                <img src="~/images/header.png" alt="Header Image" class="img-header">
                   <li class="nav-item d-flex align-items-center">
                      <p class="mb-0 mr-2">Brukernavn</p>
                      <!--Må endre denne til å peke på riktig controller relatert til log in-->
                       <a class="nav-link text-dark" asp-area="" asp-controller="Pin" asp-action="Table">Log in</a>
                   </li>
               </ul>
           </div>
       </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            <!-- Individual views will be rendered within this layout using the RenderBody() placeholder.-->
            @RenderBody()
        </main>
    </div>
    <div class="footer container d-flex justify-content-between">
        <p>Laget av Therese Trollbu, Solveig Jørgensen og Ingeborg Randen</p>
    </div>
</body>
</html>